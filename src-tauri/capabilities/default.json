{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main window. core:default covers invoke + event; shell:allow-open-url is required by the Etsy OAuth flow which opens the consent URL in the system browser.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
