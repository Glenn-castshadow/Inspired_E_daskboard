{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the main window. core:default covers invoke + event; opener:default lets the frontend open http(s) URLs in the system browser (label-print page). The Rust-side OAuth browser open doesn't go through capabilities.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
