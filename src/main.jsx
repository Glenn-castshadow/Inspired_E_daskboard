import React from "react";
import ReactDOM from "react-dom/client";

// TODO: import and wire tab shell once etsy-dashboard.jsx and fulfillment-view.jsx are in place

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <div style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>Inspired Eclectics Dashboard</h1>
      <p>Tab shell coming soon.</p>
    </div>
  </React.StrictMode>
);
