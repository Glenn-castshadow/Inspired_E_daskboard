// Your Etsy shop IDs (numeric IDs from your shop URLs or etsy.com/developers).
// Run etsy_connect(shopId) once per shop before using live data.
export const SHOP_IDS = [
  // 12345678,
  // 23456789,
  // 34567890,
];
